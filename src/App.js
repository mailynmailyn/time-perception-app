import React from 'react';
import AudioPlayer from './AudioPlayer'; // Adjust the path if necessary

const App = () => {
  return (
    <div>
      <AudioPlayer />
    </div>
  );
};

export default App;
